---
description: Generate an industry-specific sales deck (.pptx)
---
Invoke the `sales-deck` skill at `.claude/skills/sales-deck/SKILL.md`. Follow its intake workflow, synthesize slides from `knowledge-base/`, checkpoint `brief.json` with the user, and render via `scripts/build_deck.py`.
